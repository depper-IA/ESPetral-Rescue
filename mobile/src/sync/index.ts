/**
 * Módulo de sincronización — comunicación con el backend vía WebSocket
 */

export {
  SyncEngine,
  type ZoneAlert,
  type RelayMessage,
  type ConnectionState,
  type SyncEngineOptions,
  type SyncEngineListener,
} from './sync-engine.js';
